import { Typography } from '@mui/material';

const NotFound = () => {
  return (
    <Typography variant="h3" textAligm={'center'} marginTop={'150px'}>
      Sorry, this page is not found.
    </Typography>
  );
};

export default NotFound;
